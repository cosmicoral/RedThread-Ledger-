# Reference data

Place the official hackathon master-data files here locally.

Typical inputs:

- Bank account mappings
- Legal entity, investor, vendor and related-party lists
- Project code mappings
- Deal and position mappings
- Chart of accounts
- Allocation rules

Do not load the ground-truth `Staging Sheet` or `DIU` sheets into the production pipeline. Those belong in `evaluation/` only.
